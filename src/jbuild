(jbuild_version 1)

(library
 ((name        orxgboost)
  (public_name orxgboost)
  (modules (result utls gbtree))
  (libraries (dolog))))

(executables
 ((names     (test))
  (libraries (dolog batteries cpm))))
